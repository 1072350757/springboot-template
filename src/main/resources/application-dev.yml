server:
  port: 8888
  servlet:
    context-path: /


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://106.14.213.84:3306/baronj?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: rootroot
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    database: 0

mybatis-plus:
  mapper-locations: classpath:mapper/*Mapper.xml

#showSql
logging:
  level:
    com:
      baronj:
        template:
          mapper : debug